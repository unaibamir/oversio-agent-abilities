name: Dependency Review

# Scans dependency manifest changes on pull requests for known vulnerabilities
# and license problems before they can be merged.
on: [pull_request]

# Default to no permissions; grant the minimum per job.
permissions: {}

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write # Allows the action to post its summary comment.
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Dependency Review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          # Block PRs that introduce a high-severity vulnerability.
          fail-on-severity: high
          # Keep the dependency stack GPL-compatible for wordpress.org.
          deny-licenses: AGPL-3.0, GPL-3.0-only, LGPL-3.0-only
          comment-summary-in-pr: on-failure
